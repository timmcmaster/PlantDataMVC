@model PlantDataMVC.UICore.Models.ViewModels.SeedBatch.SeedBatchDeleteViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<form asp-action="Destroy" asp-controller="SeedBatch">
    <fieldset>
        <legend>Seed Batch</legend>
        @*Add hidden, as display for model doesn't show hidden input needed for submit*@
        <input asp-for="Id" type="hidden"/>
        @Html.DisplayForModel()
    </fieldset>
    <p>
        <input type="submit" value="Delete" /> |
        <a asp-controller="SeedBatch" asp-action="Index">Back to Index</a>
    </p>
</form>