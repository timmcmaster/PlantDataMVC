@using SaleEvent = PlantDataMVC.Web.Models.ViewModels.SaleEvent

@model SaleEventGridViewModel
@{
    SaleEvent.SaleEventListViewModel sampleElement = new SaleEvent.SaleEventListViewModel();
}
<div class="container-lg">
    @if (Model.Options.AllowAdd)
    {
        <a asp-controller="@PlantDataMvcAppControllers.SaleEvent" asp-action="New">Add New Item</a>
    }
    <table class="table border">
        <thead>
            <tr>
                <th scope="col">@Html.ColumnHeaderFor(m => sampleElement.Name, Model.Options.AllowSorting)</th>
                <th scope="col">@Html.ColumnHeaderFor(m => sampleElement.SaleDate, Model.Options.AllowSorting)</th>
                <th scope="col">@Html.ColumnHeaderFor(m => sampleElement.Location, Model.Options.AllowSorting)</th>
                <th scope="col"></th>
                @if (Model.Options.AllowEdit)
                {
                    <th scope="col"></th>
                }
                @if (Model.Options.AllowDelete)
                {
                    <th scope="col"></th>
                }
            </tr>
        </thead>
        <tbody>
            @{
                int count = 0;
                foreach (var entry in Model.Items)
                {
                    <tr>
                        <td><a asp-controller=@PlantDataMvcAppControllers.SaleEvent asp-action="Show" asp-route-id="@entry.Id">@entry.Name</a></td>
                        <td>@Html.DisplayFor(m => m.Items.ElementAt(count).SaleDate)</td>
                        <td>@Html.DisplayFor(m => m.Items.ElementAt(count).Location)</td>
                        <td><a asp-controller=@PlantDataMvcAppControllers.SaleEvent asp-action="Details" asp-route-id="@entry.Id">Details</a></td>
                        @if (Model.Options.AllowEdit)
                        {
                            <td><a asp-controller=@PlantDataMvcAppControllers.SaleEvent asp-action="Edit" asp-route-id="@entry.Id">Edit</a></td>
                        }
                        @if (Model.Options.AllowDelete)
                        {
                            <td><a asp-controller=@PlantDataMvcAppControllers.SaleEvent asp-action="Delete" asp-route-id="@entry.Id">Delete</a></td>
                        }
                    </tr>
                    count++;
                }
            }
        </tbody>
    </table>
    @if (Model.Options.AllowPaging)
    {
        @Html.PagingLinksFor(m => Model)
    }
</div>