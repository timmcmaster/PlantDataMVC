@model IList<PlantDataMVC.UI.Models.ViewModels.SeedTray.SeedTrayListViewModel>

@using Framework.Web.Mvc.Sorting
@using PlantDataMVC.UI.Models.ViewModels.SeedTray

@{
    SeedTrayListViewModel sampleElement = new SeedTrayListViewModel();   
}
<table class="grid">
    <tr>
        <th>@Html.ColumnHeaderFor(m => sampleElement.SeedBatchId)</th>
        <th>@Html.ColumnHeaderFor(m => sampleElement.DatePlanted)</th>
        <th></th>
        <th></th>
    </tr>

@{
int count = 0;
foreach (var model in Model) {
    <tr>
        <td>@Html.ActionLink(model.SeedBatchId.ToString(), "Show", "SeedTray", new { id = model.Id }, null)</td>
        <td>@Html.DisplayFor(m => m.ElementAt(count).DatePlanted)</td>
        <td>@Html.ActionLink("Edit", "Edit", "SeedTray", new { id = model.Id }, null)</td>
        <td>@Html.ActionLink("Delete", "Delete", "SeedTray", new { id = model.Id }, null)</td>
    </tr>
    count++;
}
}
</table>