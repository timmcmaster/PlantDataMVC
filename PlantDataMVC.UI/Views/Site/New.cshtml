@model PlantDataMVC.UI.Models.ViewModels.SiteNewViewModel

@{
    ViewBag.Title = "New";
}

<h2>New</h2>

@using (Html.BeginForm("Create","Site")) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Site</legend>

        @Html.EditorForModel()

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to Index", "Index")
</div>

