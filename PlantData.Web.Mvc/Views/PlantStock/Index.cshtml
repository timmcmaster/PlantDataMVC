@model ListViewModelStatic<PlantData.Web.Mvc.Models.ViewModels.PlantStock.PlantStockListViewModel>
@{
    ViewBag.Title = "Index";
}
<h2>Plant Stock</h2>
<p>Plant stock (from static records)</p>

@await Component.InvokeAsync("PlantStockGrid", new { model = Model, gridOptions = Model.GridOptions })
