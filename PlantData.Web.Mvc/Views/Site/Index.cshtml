@model ListViewModelStatic<PlantData.Web.Mvc.Models.ViewModels.Site.SiteListViewModel>
@{
    ViewBag.Title = "Index";
}
<h2>Site List</h2>
<p>This is a list of all seed collection sites</p>

@await Component.InvokeAsync("SiteGrid", new { model = Model, gridOptions = Model.GridOptions })
