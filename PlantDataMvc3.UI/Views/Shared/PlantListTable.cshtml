@model IList<PlantListViewModel>

@using PlantDataMvc3.UI.Models
@using PlantDataMvc3.UI.Helpers

@{
    PlantListViewModel sampleElement = new PlantListViewModel();
}
<table class="grid">
    <tr>
        <th>@Html.ColumnHeaderFor(m => sampleElement.LatinName)</th>
        <th>@Html.ColumnHeaderFor(m => sampleElement.CommonName)</th>
        <th></th>
        <th></th>
    </tr>


@{
int count = 0;
foreach (var model in Model) {
    <tr>
        <td>@Html.ActionLink(model.LatinName, "Show", "Plant", new { id = model.Id }, null)</td>
        <td>@Html.DisplayFor(m => m.ElementAt(count).CommonName)</td>
        <td>@Html.ActionLink("Edit", "Edit", "Plant", new { id = model.Id }, null)</td>
        <td>@Html.ActionLink("Delete", "Delete", "Plant", new { id = model.Id }, null)</td>
    </tr>
    count++;
}
}
</table>
